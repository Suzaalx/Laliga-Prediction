apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: laliga-ingress
  namespace: laliga-predictions
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
spec:
  ingressClassName: nginx
  rules:
  - host: laliga-predictions.local
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 8000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 3000
  # Uncomment for HTTPS with cert-manager
  # tls:
  # - hosts:
  #   - laliga-predictions.local
  #   secretName: laliga-tls-secret
---
# Optional: TLS Certificate (requires cert-manager)
# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: laliga-tls-cert
#   namespace: laliga-predictions
# spec:
#   secretName: laliga-tls-secret
#   issuerRef:
#     name: letsencrypt-prod
#     kind: ClusterIssuer
#   dnsNames:
#   - laliga-predictions.local